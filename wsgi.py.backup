import sys
import os

# Добавляем путь к вашему проекту
path = '/home/polinasrgz/RGZ-WEB-BACK'
if path not in sys.path:
    sys.path.insert(0, path)

# Указываем путь к директории с проектом
os.chdir(path)

# Импортируем Flask приложение
from app import app as application  # или замените на имя вашего модуля
